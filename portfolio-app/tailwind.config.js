/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
// This is a Tailwind CSS configuration file that specifies the content sources for Tailwind to scan for class names.
// It also extends the default theme, allowing for custom styles to be added.
